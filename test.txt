You are editing a pure PHP, FTP-only CMS.

Absolute rules:
- Pure PHP only; require_once only; no frameworks/Composer/CLI/autoloaders.
- Never touch root/config.php credentials.
- Single DB entry point: \core\Database::connection().
- UTF-8 no BOM; no closing ?>; exactly one trailing newline.
- Minimal MICRO unified-diff only; surgical edits; no reformatting.

Scope:
- /var/www/html/cms/includes/Database/Connection.php
- /var/www/html/cms/includes/core/Core/DatabaseConnection.php

Goal:
Unify all legacy `DatabaseConnection` usages by providing a single adapter class that proxies to `\core\Database::connection()` and a forwarder to avoid duplicate class definitions.

Tasks:

1) In includes/Database/Connection.php:
   - Ensure it defines class `DatabaseConnection` as a thin adapter over `\core\Database::connection()`.
   - Implement/ensure the following API (to match existing call patterns):
     • public function __construct(array $config = []): void   // ignore $config; keep for BC
     • public static function getInstance(): self               // singleton; lazy init
     • public function getConnection(): \PDO                    // instance getter
     • public static function getConnectionStatic(?string $name = null): \PDO // new compat alias
     • public static function execute(string $sql, array $params = []): bool
     • public static function fetchAll(string $sql, array $params = []): array
     • public static function fetchOne(string $sql, array $params = [])       // returns mixed/array|false
     • public static function logPerformanceMetric(string $metric, $value, array $tags = []): void
   - Inside each method, obtain `$pdo = \core\Database::connection();` and call PDO safely:
     • execute: `$stmt = $pdo->prepare($sql); return $stmt->execute($params);`
     • fetchAll: `$stmt = $pdo->prepare($sql); $stmt->execute($params); return $stmt->fetchAll(\PDO::FETCH_ASSOC);`
     • fetchOne: `$stmt = $pdo->prepare($sql); $stmt->execute($params); return $stmt->fetch(\PDO::FETCH_ASSOC);`
     • logPerformanceMetric: `error_log(json_encode([...], JSON_UNESCAPED_SLASHES));` (minimal, no new files)
   - For compatibility with existing static calls like `DatabaseConnection::getConnection($name)`, add:
     • public static function __callStatic($name, $args) { if ($name === 'getConnection') { return self::getConnectionStatic($args[0] ?? null); } /* otherwise no-op or throw */ }
   - Do NOT introduce namespaces or new includes besides `require_once __DIR__ . '/../core/Database.php';` at the top.

2) In includes/core/Core/DatabaseConnection.php:
   - Replace the entire file with a forwarder to the canonical adapter:
     • `<?php`
     • `require_once __DIR__ . '/../../Database/Connection.php';`
     • (no class declarations here)
   - This eliminates duplicate class definitions while preserving legacy include paths.

Output:
- Provide ONE unified diff covering both files only.
- No explanations or comments in the diff.
- Then write `FINAL SUMMARY:` listing:
  • methods present in the adapter,
  • confirmation that the forwarder replaces duplicate class code,
  • that changes were applied and saved.
