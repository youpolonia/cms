<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        Schema::create('version_comparison_stats', function (Blueprint $table) {
            $table->id();
            $table->foreignId('content_id')->constrained()->cascadeOnDelete();
            $table->foreignId('from_version_id')->constrained('content_versions')->cascadeOnDelete();
            $table->foreignId('to_version_id')->constrained('content_versions')->cascadeOnDelete();
            $table->unsignedInteger('changes_count')->default(0);
            $table->unsignedInteger('conflicts_count')->default(0);
            $table->unsignedInteger('resolutions_count')->default(0);
            $table->json('resolution_stats')->nullable();
            $table->json('change_types')->nullable();
            $table->timestamps();
        });
    }

    public function down()
    {
        Schema::dropIfExists('version_comparison_stats');
    }
};
