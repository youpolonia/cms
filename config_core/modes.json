{
  "modes": {
    "code": {
      "name": "💻 Code",
      "description": "Primary coding mode for PHP/JS development",
      "filePatterns": ["*.php", "*.js", "*.ts", "*.vue", "*.css", "*.scss", "*.html"],
      "progressReader": "required"
    },
    "architect": {
      "name": "🏗️ Architect",
      "description": "System architecture planning",
      "filePatterns": ["*.md", "*.json", "*.yaml"],
      "progressReader": "required" 
    },
    "ask": {
      "name": "❓ Ask",
      "description": "Question answering mode",
      "progressReader": "optional"
    },
    "debug": {
      "name": "🪲 Debug",
      "description": "Debugging and analysis",
      "filePatterns": ["*.php", "*.js", "*.log"],
      "progressReader": "required",
      "tokenManagement": {
        "monitoring": "required",
        "maxTokenUsage": 0.75,
        "chunkingThreshold": 0.5,
        "emergencyProcedures": {
          "pauseDuration": 300,
          "fallbackMode": "code",
          "rateReduction": 0.75
        },
        "rules": [
          "Monitor token usage in real-time",
          "Split large tasks when approaching 75% limit",
          "Chunk responses exceeding 50% limit",
          "Never exceed 75% token budget",
          "Immediately pause operations if limits exceeded",
          "Switch to fallback mode if needed"
        ]
      }
    },
    "orchestrator": {
      "name": "🪃 Orchestrator",
      "description": "Workflow coordination",
      "filePatterns": ["memory-bank/*", "*.workflow"],
      "progressReader": "required"
    }
  },
  "progressReader": {
    "maxLines": 50,
    "defaultBehavior": "lastEntries",
    "requiredFor": ["code", "architect", "debug", "orchestrator"]
  }
}